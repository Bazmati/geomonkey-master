{% extends 'admin/base.html.twig' %}

{% block admin_content %}
    <div class="p-4">
        <h1 class="text-success mb-4">Créer un nouvel événement</h1>

        <div class="card shadow-sm">
            <div class="card-body">
                {{ include('admin/event/_form.html.twig') }}

                <div class="mt-3">
                    <a href="{{ path('admin_events_index') }}" class="btn btn-secondary">
                        <i class="bi bi-arrow-left"></i> Retour à la liste
                    </a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
